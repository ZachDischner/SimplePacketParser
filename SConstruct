## Assuming gcc compiler environment
env = Environment(CC='gcc', CCFLAGS = "-Wall -Wextra -g -pedantic -std=iso9899:1999")
# if you have specific, nonstandard gcc installed you can do :
# env = Environment(CC='gcc-4.9', CCFLAGS ...
env.Program('packetParser.c')